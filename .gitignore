# --- secrets (NEVER commit) ---
.env
openai_key.txt
*.key

# --- python ---
__pycache__/
*.pyc
*.pyo
venv/
.venv/
.ipynb_checkpoints/

# --- heavy raw data (attach as a separate ZIP in the LMS submission) ---
data/raw/

# --- processed data: the app only needs the final table; skip the intermediates ---
data/processed/*
!data/processed/final_scouting_table.csv

# --- os noise ---
.DS_Store
Thumbs.db

# streamlit local secrets
.streamlit/secrets.toml

# --- local-only working notes / dev log (kept off GitHub on purpose) ---
DEV_LOG.md
notes_local/
Master_Scout_Presentation_Prep.docx
# personal explanation docs (Hebrew, kept local) + Word lock/temp files
הסבר מורחב על טבלאות של פרויקט סקואטינג כדורגל פאדי 2026.docx
~$*.docx
*.tmp
